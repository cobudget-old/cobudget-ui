module.exports = {
  url: '/contributors'
  template: require('./round-contributors-template.html')
  controller: require('./round-contributors-controller.coffee')
  resolve:
    members: ($stateParams, MembershipsStore) ->
      MembershipsStore.all($stateParams.groupId).then (members) ->
        members
}
